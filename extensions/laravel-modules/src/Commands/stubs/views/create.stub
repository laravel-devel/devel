@extends('develdashboard::layout')

@section('content')
    <header class="section-header">
        <div class="title">
            {{ config('$LOWER_NAME$.display_name') }} - Add
        </div>
    </header>

    <div class="section-body">
        <div class="card">
            <div class="body">
                <v-form action="{{ route('dashboard.$LOWER_NAME$.store') }}"
                    method="POST"
                    type="table"
                    :fields="{{ json_encode($form) }}"
                    success="{{ route('dashboard.$LOWER_NAME$.index') }}"></v-form>
            </div>
        </div>
    </div>
@endsection
